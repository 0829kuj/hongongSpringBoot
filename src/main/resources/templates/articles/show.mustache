{{>layouts/header}}

<div class="p-5">
    {{#article}}
        <p>{{id}}</p>
        <h4>{{title}}</h4>
        <p>{{content}}</p>
    {{/article}}
    <a href="/articles">Go to Articles List</a>
    <a href="articles/{{article.id}}/edit" class="btn btn-primary">Edit</a>
</div>
{{>layouts/footer}}
